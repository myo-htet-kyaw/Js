const pInfo = {
    firstName: 'Dylan',
    lastName: 'Israel',
    city: 'Austin',
    state: 'Teaxs',
    zipCode: 73301
};

const {firstName: fn,lastName: ln}= pInfo;

// console.log(`${firstName} ${lastName}`);`

console.log(`${fn} ${ln}`);